default: &default
  twitter:
    consumer_key: (API key)
    consumer_secret_key: (API secret key)
    access_token: (Access token)
    access_token_secret: (Access token secret)
    env_name: (ラベル名)

development:
  <<: *default

production:
  <<: *default
